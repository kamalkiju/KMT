# KMT

Knowledge / template orchestration UI (CEUI) — see [`kmt-ui/README.md`](kmt-ui/README.md) to run and build the frontend.

**Vercel:** either set **Root Directory** to `kmt-ui`, or leave root as `.` and use the repo-root `vercel.json` ([details](kmt-ui/README.md#deploy-vercel)).
